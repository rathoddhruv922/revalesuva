enum Environment {
  staging,
  uat,
  main,
}

enum ParamType {
  raw,
  formData,
  none,
}

enum QuestionType {
  closedEnded,
  mcq,
}

enum AnswerType {
  yesNo,
  yesWithInput,
  noWithInput,
  inputBox,
}

enum OvulationType {
  hormonalDays,
  ovulation,
  menstruation,
}

enum ShopType {
  product,
  ingredients,
}
